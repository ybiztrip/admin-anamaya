import Layout from '@/components/Layout';
import SectionCard from '@/components/SectionCard';

export default function HotelView() {
  return (
    <Layout>
      <SectionCard className="mt-4" title="Hotel"></SectionCard>
    </Layout>
  );
}
